import React from 'react';
import Editor from './components/Editor';
import Preview from './components/Preview';

export default class {
  static Editor: typeof Editor = Editor;
  static Preview: typeof Preview = Preview;
}
