/**
 * title: 我是标题
 * desc: 我是简介，我可以用 `Markdown` 来编写
 */
import React from 'react';
import { Link } from '@hocgin/ui';

export default () => {
  return (
    <div style={{ height: '500px' }}>
      <Link>链接</Link>
    </div>
  );
};
