import React from 'react';
import { Sponsor } from '@hocgin/ui';

export default () => {
  let styles = {
    width: '100%',
    margin: '0 auto',
    'text-align': 'center',
  };

  return (
    <div style={styles}>
      <Sponsor />
    </div>
  );
};
